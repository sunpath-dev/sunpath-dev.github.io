{
  "name": "@sunpath/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, zod schemas, scoring function, and adapter interfaces.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
